# OgameTools
오게임 자동화
### 20190309 DownloadOgameTools.py 422 에러
requests 라이브러리를 통해 Github을 로그인하려고 Request 헤더, Form을 모두 채워넣었는데도 422 에러가 떴다.  
아니, 대체 왜... 헤더도 폼도 문제가 없는데 왜인가 싶었는데, 토큰 구하는 함수가 문제가 있었다.  
기본적으로 with requests.Session() as s: 아래에서 세션을 진행했는데, token 함수는 s 세션을 쓰는 게 아니라 별도로 requests.Session().get으로 로그인 페이지의 토큰을 불러온 것이다... 즉, 나는 로그인 창 두개를 각각 띄우고 한 쪽의 토큰을 다른 쪽에 대입하고 있었던 것이다...  
프로그래머인 이상 방황하기 마련이다. 뭐, 어쨌든 제대로 작동하니까...
